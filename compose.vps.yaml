services:
  mcp:
    environment:
      LARAVEL_API_URL: "${DOCKER_LARAVEL_API_URL:-http://batix_prod_nginx/api/v1}"
    networks:
      - web

networks:
  web:
    external: true
    name: "${BATIX_WEB_NETWORK:-web}"
